<li>
    <i class="fa fa-fw fa-{{item.icon}}"></i>
    <div>
        <strong>{{item.label}}</strong>
        <span>{{item.readable}}</span>
        <input type="hidden" name="{{item.field}}" id="{{item.field}}" value="{{item.readable}}">
        <!-- TODO: Add Details for HTML5 + CSS3 -->
    </div>
</li>